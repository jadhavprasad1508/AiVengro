# Core scientific stack
pandas
numpy
scikit-learn

# Visualization (for EDA / optional)
matplotlib
seaborn

# Association rule mining
mlxtend
efficient-apriori

# Web framework (backend)
Flask
Werkzeug
Jinja2

# Environment and configuration management
python-dotenv

# API requests and progress bars
requests

# For JSON handling / serialization (standard but pinned)
orjson

# For model persistence and compression (used implicitly)
joblib


# (optional) for CORS support in Flask if needed
Flask-Cors
